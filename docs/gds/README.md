# Final GDS for Visual Inspection

This folder contains the final GDS generated by the completed local LibreLane signoff run:

- [tt_um_combolock.gds](tt_um_combolock.gds)

The local run tag is `RUN_2026-05-31_02-03-14`, recorded only for traceability. The `runs/` directory is ignored and is not used as GitHub evidence.

## TinyTapeout GDS Viewer

To inspect the layout visually:

1. Open <https://gds-viewer.tinytapeout.com/>.
2. Download `tt_um_combolock.gds` from this folder.
3. Drag and drop the GDS file into the viewer.

## KLayout

The committed GDS can also be opened locally with:

```sh
klayout docs/gds/tt_um_combolock.gds
```
